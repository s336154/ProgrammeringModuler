package Arbeidskrav4;
//Here I had to use for-løkke to be able to execute the code perfectly . .
//"Liste" is not a class so I didint have to form new inorder to avoid the red uunderline


public class Math_functions {
  /*  public static void main(String[] args) {
        int[] liste = {1, 3, 2, 5, 6};
        liste[3] = 25;
        for (int i = 0; i < 5; i++) {


            System.out.print(liste[i] + " ");
        }
    } */

    // (int) (Math.random() * 201); "To get random number less than 201"
// arbeidskrav2.oppgave2 && arbeidskrav3.oppgave1 && FXML.kodebrikke
    //  int i = 0;
    //        while(i < tabell.length) {
    //            // Trekker et tilfeldig tall mellom 100 og 999 (grensene inkluderte)
    //            int tilfeldigTall = (int)(Math.random() * 900 + 100); //the number multiplied has to be greater by 1!

    // (int) (Math.pow()); "To form exponents"
    // Math.PI "Math pi"
    // example: double areal = Math.PI * Math.pow(radius, 2);
    //String.format("%.2f", gjennomsnitt)

    // i++;   // Hvis den ikke finnes allerede, da skal vi gå til neste indeks i arrayet vårt.


}