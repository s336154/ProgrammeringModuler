package Konto2020;
/*
void btnRegistrerVær(ActionEvent event) {
        // legg inn kode her
        String dato = txtDato.getText();
        String innRegn = txtRegn.getText();
        String innTemp = txtTemp.getText();
        double regn = Double.parseDouble(innRegn);
        double temp = Double.parseDouble(innTemp);
        Observarsjoner enRegistrering = new Observarsjoner(dato,temp,regn);
        register.add(enRegistrering);
        }
@FXML
void btnSnittRegn(ActionEvent event) {
        // legg inn kode her
        double sum=0;
        for (Observarsjoner observasjon:register) {
        sum +=observasjon.getNedbør();
        }
        double snitt = sum / register.size();
        String ut = snitt+" mm";
        lblSnittRegn.setText(ut);
        }
@FXML
void btnSnittTemperatur(ActionEvent event) {
        // legg inn kode her
        double sum=0;
        for (Observarsjoner observasjon:register) {
        sum +=observasjon.getTempratur();
        }
        double snitt = sum / register.size();
        String ut = snitt+" C";
        lblSnittTemp.setText(ut);
        }
@FXML
void btnSlettRegister(ActionEvent event) {
        // legg inn kode her
        register.clear();
        }
        public class Oppgave4 {}
 */

